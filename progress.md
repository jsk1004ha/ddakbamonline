Original prompt: 온라인이 아닌 로컬 게임을 제거하고, 자기 패 이름과 간결한 족보 롤업을 추가하며, 로그인과 딱밤 장부를 각각 별도 팝업으로 분리한다.

## 2026-07-15

- A안인 온라인 허브 + 로그인 중앙 모달 + 장부 독립 모달을 선택했다.
- 패 조합 설명과 `특수패 1위` 같은 문구는 제외한다.
- 족보 롤업은 광땡·특수패를 자세히, 땡·끗은 범위로 축약한다.
- 구현 전 설계 문서를 작성했다.

## 완료

- 온라인 전용 셸과 로그인·딱밤 장부 모달 구현
- 자기 패 이름 및 간결한 족보 롤업 구현
- 로컬 전용 코드·CSS·README 문구 삭제
- 유닛·프로덕션 빌드·데스크톱/모바일 로그인 모달 검증

## Task 6 완료

- `src/app/page.tsx`: 실제 계정 방 흐름을 유일한 진입점으로 연결
- `src/components/game-table.tsx`: 중복 플레이 화면 삭제
- `src/app/globals.css`: 사용하지 않는 화면 스타일 제거 및 온라인 셸 정리
- `README.md`: 필수 Supabase 설정과 계정 방 사용 방식으로 갱신
- 검증: 사용 중단 표면 검색 0건, Node 테스트 32/32, lint, typecheck, production build, `git diff --check` 통과 (`/` 정적 생성)

## 브라우저 검증

- `develop-web-game` Playwright 클라이언트로 로그인 모달을 열고 실제 스크린샷과 콘솔 오류를 확인했다.
- 첫 캡처에서 닫기 버튼이 제목 아래로 밀린 문제를 발견해 헤더 양끝 정렬로 교정했다.
- `visual-verdict` 재검증 점수 93점으로 90점 통과 기준을 충족했다.
- 1280×720 데스크톱과 390×844 모바일에서 로그인/회원가입 탭, `Esc` 닫기, 트리거 초점 복귀, 배경 스크롤 복원을 확인했다.
- 남은 확인 범위: 실제 계정 여러 개가 동시에 접속한 쇼다운과 장부 충돌 상황은 배포 후 실사용 검증이 필요하다.

## Supabase 보정

- 최종 리뷰에서 기존 딱밤 통계 트리거가 채권자와 채무자의 누적 때림·맞음을 반대로 기록하는 결함을 발견했다.
- `correct_hit_stat_direction` 마이그레이션으로 채권자의 `hits_delivered`와 채무자의 `hits_received`가 증가하도록 교정하고 기존 합계를 방향성 의무에서 다시 계산했다.
- 원격 프로젝트 적용 후 함수 방향과 프로필 합계 불일치 0건을 SQL로 확인했으며, Supabase 보안 Advisor는 경고 0건이다.
- 알려진 구조적 위험: 현재 방 상태 JSON은 방 멤버가 갱신하는 신뢰 기반 구조라 악성 클라이언트의 상태 위조를 완전히 막지는 못한다. 서버 권위형 전환 RPC는 별도 보안 강화 작업이 필요하다.

## Task 5A 오프라인 장부 QA 준비

- 일회성 계정 간 오프라인 빚 생성, 양쪽 RLS 조회, 정밀도·통계 불변성, 거부 입력·직접 쓰기 차단, 딱밤 차감과 선행 정리를 라이브 권한 QA에 추가했다.
- 로컬 계약 테스트만 실행했으며 원격 Supabase 라이브 QA는 후속 실행으로 남겼다.

## Task 5A 원격 적용 및 시각 QA 완료

- Supabase 원격 프로젝트에 `add_offline_hit_obligations`와 `index_offline_obligation_creator` 마이그레이션을 적용하고 컬럼·제약·함수 ACL·인덱스를 다시 조회했다.
- 공개 가입으로 만든 일회성 계정 두 개로 오프라인 빚 RPC, 초대형 정수 원문 정밀도, 양쪽 RLS 조회, 프로필 계수 불변, 거부 입력 3종, 직접 쓰기 2종 차단, 실제 한 대 때림을 검증했다.
- Playwright에서 이름 검색 → 계정 선택 → 17회 추가 → `오프라인` 배지 → 한 대 때림을 실행해 남음 16·완료 1·누적 때림 1 반영과 콘솔 오류 0건을 확인했다.
- 1280×720 데스크톱 두 열과 390×844 모바일 한 열/전체 폭을 캡처해 긴 이름·횟수 줄바꿈과 44px 조작 영역을 확인했다.
- 두 차례 QA에서 만든 장부·인증 사용자·프로필은 관리 SQL로 삭제했으며 각 잔존 수가 모두 0임을 확인했다.
- Supabase Advisor의 새 외래키 인덱스 누락은 보정했다. 남은 SECURITY DEFINER 경고는 인증 사용자 전용 권한 RPC 세 개와 이번 오프라인 RPC에 대한 의도된 경고이며, 새 인덱스의 unused 정보는 생성 직후라 정상이다.

## 한글 아이디 지원

- 아이디 허용 범위를 NFC 정규화된 한글·영문·숫자·밑줄 2~20자로 확장했다.
- 기존 영문 아이디의 내부 인증 주소는 유지하고, 한글 아이디만 SHA-256 기반 ASCII 내부 주소로 변환한다.
- 가입 메타데이터의 원본 아이디는 DB 트리거가 내부 주소의 해시와 대조하므로 임의 변조로 다른 아이디를 만들 수 없다.
- Supabase 롤백 트랜잭션에서 제약·트리거 SQL을 컴파일했고, 클라이언트와 DB의 `한글아이디` 해시 결과가 일치함을 확인했다.
- 첫 라이브 가입에서 Supabase Auth가 이메일 전체를 소문자로 바꾸는 동작을 발견해 클라이언트와 DB 해시 비교를 모두 소문자로 보정하고 회귀 테스트를 추가했다.
- 원격에서 한글 아이디 가입 → 프로필 원문 보존 → 로그아웃 → 같은 아이디 재로그인을 확인했으며, 검증용 인증 사용자와 프로필은 삭제 후 잔존 0건을 확인했다.
- 로그인 팝업에서 `한글아이디`가 HTML 제약 검사를 통과하고 화면에 정상 표시되며 콘솔 오류가 없음을 Playwright와 스크린샷으로 확인했다.

## 전체 딱밤 장부

- 로그인한 사용자는 당사자가 아닌 기록까지 포함한 전체 미완료 딱밤 장부를 조회할 수 있도록 SELECT RLS를 전역 읽기로 변경했다. 쓰기와 한 대 때림 권한은 기존 서버 RPC와 채권자 검사로 계속 제한한다.
- 장부 팝업에 때릴 사람·맞을 사람 이름 검색과 `검색 결과 / 전체 건수` 표시를 추가하고, 계정 아이디는 모든 검색 결과와 장부 행에서 제거했다.
- 오프라인 빚 RPC 반환 행을 즉시 로컬 장부에 병합한 뒤 전역 목록을 다시 읽도록 바꿔 `내가 맞을` 입력이 새로고침 지연이나 일시적 조회 실패에도 바로 보이게 했다.
- 원격 Supabase에서 제3자 전역 읽기, 익명 읽기 거부, 직접 쓰기 거부, 제3자 때림 거부, 채권자 때림, `i_owe` 방향을 확인했고 QA 사용자·장부·프로필 잔존 수가 모두 0임을 확인했다.
- 실제 브라우저에서 전체 5건 표시, 이름 `민수` 검색 3건, 아이디 미노출, `내가 맞을` 4회 추가 직후 요약 3→7 반영, 성공 알림과 콘솔 오류 0건을 확인했다.
- `develop-web-game` 클라이언트 캡처와 390×844 반응형 치수 검증을 완료했다. `visual-verdict`는 93점으로 통과했다.
- 검증: Node 테스트 141/141, lint, typecheck, production build, `git diff --check` 통과.

## Tasks 4–5 진행 중

- 전용 플레이 화면과 방 수명주기 복구를 테스트 우선으로 구현한다.
- TODO: 수명주기 RPC 타입·복구 흐름·접속 확인·죽기/게임 종료·대형 반응형 테이블을 연결하고 전체 로컬 검증을 마친다.
- RED: `online-round` 기존 22개 통과, 새 계약 5개가 의도한 미구현 사유로 실패했다.
- 구현: 닫힘/탈퇴 복구, RPC 전용 나가기, 20초 접속 갱신·10초 만료 확인, 죽기/게임 종료, 접속 기반 다음 판 제한, 전용 대형 테이블을 연결했다.
- 완료: 플레이 중 로비 통계를 숨기고 테이블만 표시하는 전용 분기를 적용했다. 내 패는 132×194px(모바일 96×142px), 상대 패는 76×112px(모바일 58×86px)로 확대했고 쇼다운 전에는 뒷면 두 장만 표시한다.
- 완료: 로비 나가기와 게임 종료를 모두 수명주기 RPC로 연결하고, 방 닫힘·삭제·본인 멤버십 삭제를 한 게임 메인 복귀 경로로 처리했다.
- 완료: 20초 하트비트, 10초 유휴 만료 확인, 화면 복귀 시 즉시 갱신, 60초 접속 여부에 따른 다음 판 차단, 죽기·받기·올리기·게임 종료 제어를 연결했다.
- 로컬 검증: `online-round` 27/27, 전체 Node 테스트 180/180, lint, typecheck, production build, `git diff --check` 통과.
- 동시성 보정: 이전 계정의 느린 새로고침과 해제된 이전 방 채널 이벤트가 현재 방을 덮어쓰거나 메인으로 돌려보내지 못하게 계정 세대·현재 방 ID 검사를 추가했다. 하트비트·만료 RPC는 세대별로 중복 실행을 막고, 동시 상태 새로고침은 하나의 요청으로 합친다.
- 동시성 회귀 검증: `online-round` 30/30, 관련 계정·방 계약 37/37, 전체 Node 테스트 183/183, lint, typecheck, production build 통과.
